[core]
        excludesfile = ~/dotfiles/git/gitignore
        attributesfile = ~/dotfiles/git/gitattributes
        pager = diff-highlight | less -r
        ignorecase = false

[user]
        name = l5x
        email = 189198+l5x@users.noreply.github.com
        signingkey = 0x0DB1B2FEE81674CB

[push]
        default = simple
        followTags = true

[credential]
        helper = cache --timeout=36000

[color]
        ui = always

[color "branch"]
        current = yellow reverse
        local = yellow
        remote = green

[color "diff"]
        meta = yellow bold
        frag = magenta bold
        old = red bold
        new = green bold

[color "status"]
        added = yellow
        changed = green
        untracked = cyan

[color "diff-highlight"]
       oldNormal = "red bold"
       oldHighlight = "red bold 52"
       newNormal = "green bold"
       newHighlight = "green bold 22"

[status]
        relativePaths = true

[url "git@github.com:"]
        insteadOf = "gh:"
        insteadOf = "git://github.com"
        pushInsteadOf = "github:"
        pushInsteadOf = "git://github.com/"
        pushInsteadOf = "https://github.com/"

[url "git@gist.github.com:"]
        insteadOf = "gst:"
        pushInsteadOf = "gist:"
        pushInsteadOf = "git://gist.github.com/"
        pushInsteadOf = "https://gist.github.com/"

[alias]
        c = commit -a -s
        p = push
        l = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
        s = status -sb
        t = grep -iEI "TODO|FIXME"
        co = checkout
        ec = config --global -e
        up = !git pull --rebase --prune $@ && git submodule update --init --recursive
        ow = !git fetch --all && git reset --hard origin/master && git submodule update --init
        who = "!sh -c 'git log -i -1 --pretty=\"format:%an <%ae>\n\" --author=\"$1\"' -"
        wip = !git add -u && git commit -m "WIP"
        save = !git add -A && git commit -m 'SAVEPOINT'
        undo = reset HEAD~1 --mixed
        wipe = !git add -A && git commit -qm 'WIPE SAVEPOINT' && git reset HEAD~1 --hard
        amend = commit -a --amend
        serve = !git daemon --reuseaddr --verbose  --base-path=. --export-all ./.git
        sclone = clone --depth=1
        # bclean = "!f() { git branch --merged ${1-master} | grep -v " ${1-master}$" | xargs -r git branch -d; }; f"
        # bdone = "!f() { git checkout ${1-master} && git up && git bclean ${1-master}; }; f"
        # lc = log ORIG_HEAD.. --stat --no-merges
        # gl = log --oneline --decorate --stat --graph
        # prune-all = !git remote | xargs -n 1 git remote prune
        # whatis = show -s --pretty='tformat:%h (%s, %ad)' --date=short
[gpg]
        program = gpg2
[pull]
        rebase = true
